<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <title>Batch Add</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 min-h-screen">
  
  <%- include('Header'); -%>
  <div class="max-w-lg mx-auto py-10">
    <h1 class="text-2xl font-bold mb-6 text-center text-indigo-600">Edit Batch</h1>

    <form action="/batch/edit/<%= batch._id %>" method="POST" class="bg-white p-6 rounded-lg shadow-md space-y-4">
      <div>
        <label class="block text-gray-700 mb-2">Course</label>
        <input type="text" name="course" required class="w-full border border-gray-300 p-2 rounded" value="<%= batch.course %>" />
      </div>
      <div>
        <label class="block text-gray-700 mb-2">Start Date</label>
        <input type="date" name="startDate" required class="w-full border border-gray-300 p-2 rounded" value="<%= batch.startDate %>" />
      </div>
      <div>
        <label class="block text-gray-700 mb-2">Days</label>
        <input type="text" name="days" class="w-full border border-gray-300 p-2 rounded" value="<%= batch.days %>" />
      </div>
      <div>
        <label class="block text-gray-700 mb-2">Time</label>
        <input type="time" name="time" required class="w-full border border-gray-300 p-2 rounded" value="<%= batch.time %>" />
      </div>

      <div class="text-center space-x-4">
        <button type="submit" class="bg-indigo-600 text-white px-4 py-2 rounded hover:bg-indigo-700">Update</button>
        <a href="/batch" class="inline-block bg-gray-300 text-gray-700 px-4 py-2 rounded hover:bg-gray-400">Cancel</a>
      </div>
    </form>
  </div>
</body>

</html>
